# Copy this file to .env (never commit .env itself) and adjust as needed.

PROJECT_NAME= Adaptive-Ai-Model

VERSION=0.1.0
DEBUG=True

# Relative sqlite path - keep it relative so the app stays offline and
# portable across every teammate's machine.
DATABASE_URL=sqlite:///./data/app.db